spring:
  application:
    name: "BoxerClubBg"
  security:
    filter:
      order: 10
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
  sql:
    init:
      mode: never
#  datasource:
#    driver-class-name: local.hsqldb.jdbc.JDBCDriver
#    url: jdbc:hsqldb:hsql://localhost/testdb.syntax_mys=true;DB_CLOSE_DELAY=-1
#    username: "SA"
app:
  admin:
    password: "1111"
  token:
    secretKey: "ghjhgjgjhl"

cloudinary:
  cloud-name: "CLOUD_NAME"
  api-key: "CLOUD_API_KEY"
  api-secret: "CLOUD_API_SECRET"

mail:
  host: "localhost"
  port: 1025
  username: "mobilele@mobilele.com"
  password: "topsecret"